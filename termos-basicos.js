//estudando a formatação de linha. Quando entramos com duas barras a linha é comentada

/*quando entramos com barra e asterisco
o comentário se extende e só para quando eu 
fecho com asterisco e barra */

// em uma equação em que o valor é mutavel usamos a variável, pois esse valor pode ser manipulado para encontrar vários resultados.

/*var preco = 3;
var desconto = 1;
preco = preco - desconto;/*

/* no caso de constantes o valor não é mutável, na equação de 1 grau utilizamos o preço como constante e o desconto como variável.
Ao utilizar a constante o seu nome deve ser utilizado como caixa alta por regra de bons costumes utilizando a linguagem. 
Criamos outra variável que é o total pois ela será dinamica ao passo que alteramos o valor do desconto */

/*const PRECO = 3;
var desconto = 1;
var total = PRECO - desconto;*/

/* Função entre parenteses declaramos os valores os quais serão calculados. No caso abaixo a soma. Entre chaves determinamos a operação e também podemos ver o resultado no console com o console.log */


/* function soma (a, b) {
    return a + b; //8
}*/

/* Abaixo precisamos chamar os valores para que a equação aconteça.
Atenção!!! Caso não chame a função o cálculo não acontece*/

//soma (3, 5);

/*Executando um Arquivo Js.
A partir da instalação do Node.js. Abrimos ot terminal aonde desejamos. O comando utilizado é o console.log*/

/*comando para teste abaixo: entre parenteses escrevemos o que queremos escrito no terminal.
Comando para rodar no terminal é 'node e o nome do arquivo' no caso a resposta foi o Hello World
console.log('Hello World');*/

